<resources>
    <!-- Base Application -->
    <style name="AppTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimary</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <!-- Launcher Splash Screen -->
    <style name="AppTheme.Launcher" parent="AppTheme">
        <item name="android:windowBackground">@drawable/launcher_screen</item>
        <item name="colorPrimaryDark">@color/colorAccent</item>
    </style>

    <!-- ToolBar -->
    <style name="AppTheme.ToolBar">
        <item name="android:background">@color/toolbarBackground</item>
        <item name="android:textColor">@color/toolbarTextColor</item>
        <item name="android:textColorPrimary">@color/toolbarTextColor</item>
        <item name="android:textColorSecondary">@color/toolbarTextColor</item>
        <item name="android:textColorHint">@color/toolbarTextColor</item>
    </style>

    <!-- Floating Action Buttons -->
    <style name="AppTheme.Fab" parent="Widget.MaterialComponents.FloatingActionButton">
        <item name="tint">@android:color/white</item>
    </style>

    <style name="AppTheme.Fab.Action" parent="AppTheme.Fab">
        <item name="backgroundTint">@color/colorFabAction</item>
    </style>

    <style name="AppTheme.Fab.Mini" parent="AppTheme.Fab">
        <item name="backgroundTint">@color/colorFabMini</item>
    </style>

</resources>
